<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    tools:context=".ui.SplashScreen">

    <ImageView
        android:id="@+id/logo_coderstory"
        android:layout_width="305dp"
        android:layout_height="305dp"
        android:src="@drawable/ic_logo_coderstory"
        android:layout_marginTop="110dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/text_coderstory"
        android:layout_width="278dp"
        android:layout_height="88dp"
        android:layout_marginTop="32dp"
        android:src="@drawable/txt_coderstory"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/logo_coderstory" />

    <TextView
        android:id="@+id/slogan_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:text="@string/motto"
        android:textColor="@color/white"
        android:fontFamily="@font/poppins_bold"
        android:textSize="14sp"
        android:textAlignment="center"
        android:layout_marginTop="23dp"
        app:layout_constraintTop_toBottomOf="@id/text_coderstory"/>

</androidx.constraintlayout.widget.ConstraintLayout>